server:  port: 8080spring:  main:    allow-bean-definition-overriding: true  #允许 多个接口上的@FeignClient(“相同服务名”)。不配置后报错  application:    name: consumer-service  boot:    admin:      client:        url: http://localhost:8769eureka:  #修改eureka服务那边注册名字  instance:#    instance-id: service-ribbon-8080#    prefer-ip-address: true    leaseRenewalIntervalInSeconds: 10    health-check-url-path: /actuator/health  client:    registryFetchIntervalSeconds: 5    service-url:      defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8761}/eureka/#     defaultZone: http://localhost:8761/eureka/#springboot-adminmanagement:  endpoints:    web:      exposure:        include: "*"  endpoint:    health:      show-details: ALWAYSfeign:  hystrix:    enabled: true